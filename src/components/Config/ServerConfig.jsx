import React from 'react'
import "./ServerConfig.scss"

function ServerConfig({ws}) {
    return (
        <div className="server-config"> 
            config page
        </div>
    )
}

export default ServerConfig
